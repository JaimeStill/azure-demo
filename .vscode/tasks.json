{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cli-build",
            "command": "dotnet",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/src/cli"
            },
            "args": [
                "build"
            ]
        }
    ]
}